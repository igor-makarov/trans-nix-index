name: Set up native Nix and mise
description: Install sandboxed Nix on the runner; keep Docker tooling local-only.
runs:
  using: composite
  steps:
    - uses: cachix/install-nix-action@v31
    - uses: jdx/mise-action@v4
      with:
        install: false
